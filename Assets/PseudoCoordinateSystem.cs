using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PseudoCoordinateSystem : MonoBehaviour
{
    //TODO: einstellen, wenn eins angeklickt wird, dann deselektiere die anderen
    public bool XYebene = true;
    public bool XZebene = false;
    public bool YZebene = false;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
